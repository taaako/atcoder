#include <bits/stdc++.h>
using namespace std;

int main(){
    int apple, app;
    cin >> apple >> app;

    int applepie = 0;
    applepie = (apple * 3 + app) / 2;

    cout << applepie << endl;
}